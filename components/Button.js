/*
 * Module dependencies
 */

import React from 'react';

let Button = React.createClass({
  render: function () {
    return (
        <button>Load More</button>
    );
  }
});

export default Button;
